## What does this change?

## How was it tested?
- GNOME Shell version:
- Session: Wayland / X11

## Checklist
- [ ] User-visible strings are wrapped in `_()` and `make update-po` was run
- [ ] Every signal, timeout and actor created in `enable()` is cleaned up in `disable()`
- [ ] `CHANGELOG.md` updated (if user-visible)
- [ ] Screenshot attached (if UI changed)
